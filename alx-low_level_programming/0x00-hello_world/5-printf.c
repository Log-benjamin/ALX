#include <stdio.h>
/**
 * main - Entry point
 * Hello,print f task
 * Return: return zero when execution end
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
