#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @x: passed integer
 * Return: willl be returned an integer value
 */
int _abs(int x)
{
	if (x < 0)
		return (x * -1);
	else
		return (x);

}
